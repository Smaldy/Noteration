/**
 * Developer mode for the arcade. Flip `DEV_MODE` to `true` for local testing:
 * it surfaces a small dev panel in the cabinet that grants effectively-infinite
 * coins + score (so you can start runs and buy the whole shop) and resets owned
 * upgrades back to level 0. Leave it `false` for normal play — never ship `true`.
 *
 * Toggle by editing the line below (ask me to set it TRUE or FALSE).
 */
export const DEV_MODE = true;

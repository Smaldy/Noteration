"""Pipeline stages — one module per stage (ingestion, structure, formula, ...).

Each stage is a pure-ish service that the queue/routers drive; see
docs/architecture.md and docs/project-structure.md.
"""

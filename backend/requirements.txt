# Noteration backend dependencies. Provider SDKs (google-genai, ollama) are
# still deferred to Phase 7, when the real calls are wired.

# Phase 1 — scaffold/API/persistence
fastapi
uvicorn[standard]
sqlalchemy
alembic
pydantic
pydantic-settings
python-multipart  # multipart/form-data for PDF upload (Phase 6)

# Phase 5 — ingestion (PDF → markdown + page renders)
markitdown[pdf]
pymupdf

# Audio transcription — silence trim + chunking (bundles a static ffmpeg binary,
# no system install, fully offline). See services/pipeline/audio_chunking.py.
imageio-ffmpeg

# Phase 7 — provider SDKs (lazily imported; see services/providers/)
google-genai
ollama

# Local AI setup — hardware detection (services/local_ai/hardware.py). psutil
# reads system RAM cross-platform; nvidia-ml-py is the official NVML binding.
# Both are lazily imported and their absence degrades to an "unknown" probe,
# never to "no GPU" (absence is not zero).
psutil
nvidia-ml-py

# Desktop delivery — native window for the packaged app (WebView2 on Windows,
# WebKit on macOS). Only used by packaging/launcher.py, not the test suite.
pywebview

# Test/dev
pytest
httpx

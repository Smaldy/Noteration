# Noteration backend dependencies. Provider SDKs (google-genai, anthropic,
# ollama) are still deferred to Phase 7, when the real calls are wired.

# Phase 1 — scaffold/API/persistence
fastapi
uvicorn[standard]
sqlalchemy
alembic
pydantic
pydantic-settings
python-multipart  # multipart/form-data for PDF upload (Phase 6)

# Phase 5 — ingestion (PDF → markdown + page renders)
markitdown[pdf]
pymupdf

# Phase 7 — provider SDKs (lazily imported; see services/providers/)
google-genai
anthropic
ollama

# Test/dev
pytest
httpx
